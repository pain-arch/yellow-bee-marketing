import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Yellow Bee Marketing | Digital Growth Agency",
  description:
    "Yellow Bee Marketing builds brand strategies and digital experiences that drive measurable growth.",
  applicationName: "Yellow Bee Marketing",
  openGraph: {
    title: "Yellow Bee Marketing | Digital Growth Agency",
    description:
      "Smart brand strategies and digital experiences designed for measurable growth.",
    siteName: "Yellow Bee Marketing",
    type: "website",
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
